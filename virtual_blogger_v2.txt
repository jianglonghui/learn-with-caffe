重构虚拟博主系统，每个博主是一个拿着剧本的个体，他的剧本在生成的时候已经写好了，剧本包含学习目标、学习路径、性格特点，由ai生成。学习路径为学习目标对应的学习大纲，学习大纲可分为几个模块，每个模块有几个小节。每节都有一个序号作为索引。
生成之后，加入博主库，由一个管理系统调度，默认会调度一次，每次调度会根据他当前的学习小节、性格特点由ai生成该次路径的学习总结并发表为简短的推文和系统的长文，以及"pass/not pass"的字段，系统根据该字段判断他是否进行到下一阶段。博主有一个催更按钮，用户点击一次，就会主动发起一次调度，更新一下文章和学习进度。
学习路径对用户不可见。
